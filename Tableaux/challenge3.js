const prompt= require("prompt-sync")();


let nombre=parseInt(prompt("combien le nombre élément afficher "));

const tableaux=[];
let valeur = 0;
for(let i=0;i<nombre;i++)
    {
    tableaux[i]=(parseInt("entrer les élément du tableau"));
    valeur =   valeur + tableaux[i];
    
}
